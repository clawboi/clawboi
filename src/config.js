export const CONFIG = {
  baseW: 320,
  baseH: 180,

  // scaling rules (canvas is base * scale)
  minScale: 3,
  maxScale: 7,

  // fixed timestep base
  dtCap: 1/30,

  // visual
  bg: "#050508",
  violet: "#8a2eff",
};

